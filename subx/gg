#!/usr/bin/env zsh
# Run git commands on a specific example's generated binary.

if [ $# -eq 0 ]
then
  echo "g <git command> <args> <target>"
  exit 1
fi

arg=( $* )
target=$arg[${#arg[@]}]
unset "arg[${#arg[@]}]"
git ${arg[@]} ./$target
