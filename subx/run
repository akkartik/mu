#!/usr/bin/env zsh
# Simulate a specific example using subx.

if [ $# -eq 0 ]
then
  echo "run <binary>"
  exit 1
fi

if [[ $1 == 'test'* ]]
then
  CFLAGS=-g subx run teensy/$1
  exit $?
fi

CFLAGS=-g subx run $1
exit $?
