language: cpp

addons:
  apt:
    sources:
      - llvm-toolchain-precise
      - ubuntu-toolchain-r-test
    packages:
      - valgrind
      - libstdc++-5-dev  # default version is not valgrind-clean

before_install:
  - CXX=clang++

script:
  - ./test_all_layers

notifications:
  email:
    on_success: never
    on_failure: always
